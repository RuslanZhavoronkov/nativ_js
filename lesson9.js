//lexical environment/зыкание/рекурсия

const globalLE = {
    environmentRecords: {
//записыв переменные на момент старта
    },
    outer: null //ссылка null (если нету внешнего лексич окружения) 
}
let car = 'bmw'


function startEngine() {     //function declaration (подлежит всплытию)
    console.log(`Start ${bmw}`)
}

con
const foo = () => {}  //fu

//замыкание

const count = 


//рекурсия

const pow = (x,n) => {
    if (n===1) {
        return x
    }
    return x * pow(x, n-1)
}



let count = 0;

const foo = ()=> {
    console.log
}